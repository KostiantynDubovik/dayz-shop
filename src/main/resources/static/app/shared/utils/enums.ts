export enum POSITION {
    LEFT = "left",
    RIGHT = "right",
    TOP = "top",
    BOTTOM = "bottom"
}

export enum TEXT_TRANSFORM {
    NONE = "none",
    CAPITALIZE = "capitalize",
    UPPERCASE = "uppercase",
    LOWERCASE = "lowercase"
}